apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: memcached-chart
spec:
  interval: 1h
  url: oci://registry-1.docker.io/bitnamicharts/memcached
  ref:
    semver: 8.3.15 # {"$imagepolicy": "apps:backend-memcached:tag"}
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: memcached
spec:
  releaseName: memcached
  chartRef:
    kind: OCIRepository
    name: memcached-chart
  interval: 1h
  timeout: 10m
  driftDetection:
    mode: disabled
  install:
    strategy:
      name: RetryOnFailure
  upgrade:
    strategy:
      name: RetryOnFailure
  # Default values
  # https://artifacthub.io/packages/helm/bitnami/memcached?modal=values
  values:
    clusterDomain: cluster.local
    global:
      security:
        allowInsecureImages: true
    image:
      repository: bitnamilegacy/memcached
    metrics:
      image:
        repository: bitnamilegacy/memcached-exporter
    volumePermissions:
      image:
        repository: bitnamilegacy/os-shell
